# 上传功能联调记录

日期：2026-09-10

## 接入范围

- 浏览器通过本网站 `/api/upload` 上传原始文件，由 Node.js 服务转发给运维提供的 Up 服务。
- 当前旁路地址：`http://210.41.33.190:31011/up`。直连旁路无需上传 AK/SK，不向 Up 发送聊天 API 密钥。
- 上传结果转换为 `Name`、`Path`、`Size`、`Url`，通过聊天接口的 `QueryExtends.Files` 传给平台。
- 支持图片、PDF、Word、PPT、TXT 的传输。内容解析、题目识别、课程路由及知识库写入仍由平台工作流执行。

## 已验证

| 项目 | 方法 | 实际结果 |
| --- | --- | --- |
| 自动测试 | `npm test` | 31 项通过，包含原有数学渲染和 WebSDK 测试 |
| 真实上传与回读 | `node scripts/check-upload.mjs --live --local=http://localhost:5174` | 经本地接口上传 120 字节合成中文文本，下载字节完全一致 |
| 图片讲题 | 上传 `test/fixtures/upload-question.png`，输入“请讲解我上传图片里的第1题和第2题。” | 智能体识别出 `f(x) = x^2` 求导与 `7 + 8` 两道题并给出讲解，确认图片内容已进入工作流 |
| 普通附件选择 | 点击输入框旁的附件按钮 | 允许多文件，不出现知识库选择框 |
| 资料上传选择 | 点击“资料上传”，选择合成测试文本 | 仅允许单文件，出现目标知识库选项 |
| 未选目标知识库 | 资料上传模式下直接发送 | 阻止提交，并提示选择目标知识库 |
| 取消请求 | 选择测试文件并发送，点击停止 | 请求取消，附件及输入保留，可再次发送 |
| 移动端控件边界 | 390 x 844 视口检查 | 附件状态、目标知识库选项、输入框均在页面宽度内，无页面横向溢出 |
| 异常响应 | 使用本地模拟服务 | 超大文件返回 JSON 413；空文件拒绝；鉴权失败、错误 JSON、摘要不一致、超时及取消均有测试覆盖 |

以上均使用人工构造的测试文件，没有上传用户私人文档，也没有写入或修改课程知识库。

## 尚未通过或未验证

## 2026-09-13 当前诊断

- 学校平台 API `https://ai.yznu.edu.cn` 可正常建立连接。
- 当前配置的 Up 地址 `http://210.41.33.190:31011/up` 在本机 TCP 连接超时，未收到 HTTP 响应；因此本次上传失败发生在到达 Up 服务之前，不是图片格式、API 密钥或知识库工作流问题。
- 待运维确认：Up 服务是否仍监听 `31011`、服务器防火墙/安全组是否放行、当前来源 IP 是否在白名单，以及该地址是否仅限校园内网访问。

1. 通用文档读取：上传 `test/fixtures/upload-sample.txt` 后，询问文件内的测试编号和课程名称。平台返回功能介绍，没有返回测试编号。当前不能宣称普通文档内容读取已完整可用；应在平台运行记录中检查意图路由、附件参数绑定、文档解析节点与正文变量传递。图片测试成功，不代表各类文档都能解析。
2. 知识库写入：只验证前端选库和附件发送协议，没有实际执行写入。需使用允许入库的课程文档验证目标库、写入状态及后续检索结果。
3. PDF、Word、PPT：格式允许上传，但尚未逐一验证平台解析结果；扫描型文件是否支持 OCR 以平台实际节点能力为准。
4. V4 签名：保留签名模式并通过确定性测试；本次真实连接使用直连旁路，没有对带签名的远端环境做验收。
5. 既有数学渲染：图片讲题返回了与平台约定不同的 LaTeX 表达，页面个别公式仍有显示问题。本次未修改数学转换模块，应单独处理。

## 运行与安全

- 本次新版服务运行于 `http://localhost:5174/`，未中断原有 5173 服务。以后按 README 启动时，使用实际配置的端口。
- `.env` 已配置 Up 地址，原有聊天 API 密钥未改变；不要提交该文件。
- 临时文件默认保存 3 小时。需要长期保留附件时，应明确保留策略；平台知识库保存是否独立于原附件必须实际验证。
- 当前 Up 使用 HTTP 旁路。正式部署前请运维限制来源 IP，提供 HTTPS 或受保护的网络通道；公网前端应配置登录和限流。
